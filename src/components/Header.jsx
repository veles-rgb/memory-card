import '../styles/Header.css';

function Header() {
  return (
    <header>
      <h1>Knockout Recall</h1>
      <p>
        Click on each UFC fighter only once to rack up points — but choose
        carefully. One wrong move, and it’s game over!
      </p>
    </header>
  );
}

export default Header;
